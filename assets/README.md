# Shared course assets

Place images and other reusable local media for all slide decks in this folder.

Reference files from any `.qmd` deck with a project-root path, for example:

```markdown
![](/assets/filename.png)
```

Only add files that are used by the course slides. Keep the larger master image archive in Dropbox.
